// custom.ts
// Custom MakeCode extension file

//% weight=200 color="#0000FF" icon="brain" block="Hour of AI"
namespace Hour_of_AI {
    /**
     * Returns a greeting message.
     */
    //% block
    export function greet(name: string): string {
        return `Hello, ${name}!`;
    }
}
